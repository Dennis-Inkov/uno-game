services:
  - type: web
    name: uno-server
    runtime: node
    rootDir: .
    buildCommand: npm run build:shared && npm run build:server
    startCommand: node server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_URL
        sync: false
